# 🚀 دليل التشغيل السريع
# Quick Start Guide

## ✅ تم الإعداد! المشروع جاهز للتشغيل

---

## 📋 الخطوات التالية:

### 1. تنفيذ SQL Schema في Supabase

#### الطريقة الأولى: عبر SQL Editor

1. افتح Supabase Dashboard: https://supabase.com/dashboard
2. اختر مشروعك
3. اذهب إلى **SQL Editor**
4. انسخ محتوى الملف `supabase/migrations/001_initial_schema.sql`
5. الصقه في SQL Editor
6. اضغط **Run**

#### الطريقة الثانية: عبر Terminal (إذا كنت تستخدم Supabase CLI)

```bash
supabase db push
```

---

### 2. إضافة بيانات تجريبية (اختياري)

في **SQL Editor**، نفذ محتوى:
```
supabase/migrations/002_sample_data.sql
```

هذا سيضيف 5 مؤسسات تجريبية.

---

### 3. إنشاء مستخدم Admin للتجربة

#### في Supabase Dashboard:

1. اذهب إلى **Authentication** > **Users**
2. اضغط **Add User** > **Create new user**
3. أدخل:
   - Email: `admin@pwa.ps`
   - Password: `Admin@123456`
   - ✅ Auto Confirm User
4. اضغط **Create User**

#### في SQL Editor، نفذ:

```sql
-- أضف المستخدم إلى جدول users
INSERT INTO public.users (id, email, full_name, phone_number, role, status, profile_completed)
VALUES (
  (SELECT id FROM auth.users WHERE email = 'admin@pwa.ps'),
  'admin@pwa.ps',
  'مدير النظام',
  '0599000000',
  'admin',
  'active',
  true
);
```

---

### 4. تشغيل المشروع

```powershell
npm run dev
```

المشروع سيعمل على: **http://localhost:3000**

---

### 5. تسجيل الدخول

1. افتح: http://localhost:3000
2. ستتحول تلقائياً إلى `/ar/dashboard`
3. إذا لم تكن مسجلاً، اذهب إلى `/ar/login`
4. سجل دخول بـ:
   - Email: `admin@pwa.ps`
   - Password: `Admin@123456`

---

## 🎯 الصفحات الجاهزة:

- ✅ `/ar/login` - تسجيل الدخول
- ✅ `/ar/dashboard` - لوحة التحكم الرئيسية
- ✅ `/ar/trainees` - إدارة المتدربين
- ✅ `/en/dashboard` - النسخة الإنجليزية

---

## 📊 الميزات الحالية:

### ✅ جاهز للاستخدام:
1. ✅ نظام المصادقة
2. ✅ لوحة تحكم مع إحصائيات KPIs
3. ✅ جدول المتدربين مع بحث
4. ✅ تبديل اللغة (عربي/إنجليزي)
5. ✅ Sidebar و Navbar
6. ✅ قاعدة بيانات كاملة

### 🔜 قيد التطوير (يمكن إضافتها):
1. نموذج إضافة متدرب جديد
2. صفحة تفاصيل المتدرب
3. إدارة المؤسسات
4. إدارة المشرفين
5. نظام التقارير
6. نظام المهام
7. تصدير Excel/CSV

---

## 🐛 حل المشاكل الشائعة:

### مشكلة: لا يمكن تسجيل الدخول

**الحل:**
1. تأكد من تنفيذ Schema SQL
2. تأكد من إضافة المستخدم في جدول `users`
3. تحقق من `.env.local` يحتوي على keys صحيحة

### مشكلة: Database errors

**الحل:**
1. تحقق من تفعيل RLS Policies
2. تأكد من تنفيذ كل SQL في 001_initial_schema.sql
3. راجع Supabase Logs في Dashboard

### مشكلة: الصفحات فارغة

**الحل:**
1. أضف بيانات تجريبية من 002_sample_data.sql
2. أو أنشئ مستخدمين يدوياً

---

## 📁 هيكل المشروع:

```
pwatrain/
├── app/[locale]/
│   ├── dashboard/          ✅ لوحة التحكم
│   ├── trainees/           ✅ إدارة المتدربين
│   ├── login/              ✅ تسجيل الدخول
│   └── ...
├── components/
│   ├── layout/             ✅ Sidebar, Navbar
│   ├── dashboard/          ✅ Stat Cards, Activity
│   ├── trainees/           ✅ Trainees Table
│   └── ui/                 ✅ Button, Card, Input...
├── lib/
│   ├── supabase/           ✅ Client, Auth
│   └── utils.ts            ✅ Helper functions
├── messages/
│   ├── ar.json             ✅ الترجمة العربية
│   └── en.json             ✅ الترجمة الإنجليزية
├── supabase/migrations/
│   ├── 001_initial_schema.sql    ✅ قاعدة البيانات
│   └── 002_sample_data.sql       ✅ بيانات تجريبية
└── .env.local              ✅ متغيرات البيئة
```

---

## 🎨 المرحلة التالية:

إذا كنت تريد إكمال التطوير، يمكنني إضافة:

1. **نموذج إضافة متدرب** - مع جميع الحقول
2. **صفحة تفاصيل المتدرب** - عرض كامل
3. **إدارة المؤسسات والمشرفين**
4. **نظام التقارير الكامل**
5. **نظام المهام**
6. **التصدير إلى Excel/CSV**

---

## 📞 جاهز للاستخدام!

المشروع الآن جاهز للتشغيل والاختبار.

**شغّل الأمر:**
```bash
npm run dev
```

**ثم افتح:** http://localhost:3000

---

**بالتوفيق! 🚀**
